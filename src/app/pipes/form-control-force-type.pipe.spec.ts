import { FormControlForceTypePipe } from './form-control-force-type.pipe';

describe('FormControlForceTypePipe', () => {
  it('create an instance', () => {
    const pipe = new FormControlForceTypePipe();
    expect(pipe).toBeTruthy();
  });
});
